const expect = require('chai').expect
const program = require('../modules/program.js')

describe('the program module', () => {

    it('should exist', () => {
        expect(program).to.be.defined
    })
})

